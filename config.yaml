target:
  mode: ssh
  host: 10.1.90.178
  user: root
  private_key_path: /root/.ssh/id_rsa
  port: 22
  connect_timeout_seconds: 5

service:
  name: nginx

policy:
  allow_reload: false
  allow_restart: false
  allow_reboot: false
  rollback_required: true
  max_iterations: 10
  benchmark_stability_threshold: 0.10

benchmark: {}
